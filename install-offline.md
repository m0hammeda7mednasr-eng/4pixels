# 📦 تثبيت المكتبات بدون نت

## المشكلة
مش قادر تثبت المكتبات لأن النت مش شغال.

## الحل

### الطريقة 1️⃣: استخدم نت مؤقت (أفضل حل)
- روح مكان فيه واي فاي
- أو استخدم موبايل هوت سبوت
- نزل المكتبات مرة واحدة
- بعدها تشتغل offline عادي

### الطريقة 2️⃣: استخدم CDN (للفرونت إند فقط)

بدل ما تستخدم npm، استخدم React من CDN مباشرة.

هعملك نسخة HTML بسيطة تشتغل بدون npm!

### الطريقة 3️⃣: حمل المكتبات من جهاز تاني
1. على جهاز فيه نت، نزل المكتبات:
```bash
cd server
npm install
cd ../client  
npm install
```

2. انسخ المجلدات دي:
- `server/node_modules/`
- `client/node_modules/`

3. حطهم في مشروعك

---

## 📋 قائمة المكتبات المطلوبة

### Backend (Server):
```json
{
  "express": "^4.18.2",
  "bcryptjs": "^2.4.3",
  "jsonwebtoken": "^9.0.2",
  "cors": "^2.8.5",
  "dotenv": "^16.3.1",
  "multer": "^1.4.5-lts.1",
  "express-validator": "^7.0.1"
}
```

### Frontend (Client):
```json
{
  "react": "^18.2.0",
  "react-dom": "^18.2.0",
  "react-router-dom": "^6.20.0",
  "axios": "^1.6.2",
  "framer-motion": "^10.16.16",
  "react-icons": "^4.12.0",
  "react-toastify": "^9.1.3",
  "jwt-decode": "^4.0.0",
  "react-scripts": "5.0.1"
}
```

---

## 🚀 حل بديل: نسخة HTML بسيطة

هعملك نسخة HTML/CSS/JS بسيطة تشتغل بدون npm خالص!
