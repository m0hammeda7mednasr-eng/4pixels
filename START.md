# 🚀 دليل التشغيل السريع - Four Pixels

## 📋 المتطلبات
- Node.js (v14 أو أحدث)
- npm أو yarn

## ⚡ خطوات التشغيل

### 1️⃣ تشغيل السيرفر (Backend)

```bash
cd server
npm install
npm start
```

السيرفر سيعمل على: `http://localhost:5001`

### 2️⃣ تشغيل الواجهة (Frontend)

في نافذة terminal جديدة:

```bash
cd client
npm install
npm start
```

الموقع سيفتح على: `http://localhost:3000`

## 🔐 تسجيل الدخول للأدمن

بعد تشغيل الموقع:

1. اذهب إلى: `http://localhost:3000/login`
2. استخدم البيانات التالية:
   - **Email:** `admin@4pixels.com`
   - **Password:** `admin123`

## 📁 هيكل المشروع

```
four-pixels/
├── client/              # React Frontend
│   ├── src/
│   │   ├── pages/      # الصفحات (Home, Admin, Login, etc.)
│   │   ├── components/ # المكونات (Header, Footer)
│   │   ├── context/    # Context API (Auth, Language, Theme)
│   │   └── services/   # API calls
│   └── package.json
│
├── server/              # Node.js Backend
│   ├── routes/         # API Routes
│   ├── middleware/     # Auth middleware
│   ├── data/           # JSON Database
│   └── package.json
│
└── simple-version/     # نسخة HTML بسيطة
```

## 🎯 الصفحات المتاحة

- `/` - الصفحة الرئيسية
- `/about` - من نحن
- `/services` - الخدمات
- `/projects` - المشاريع
- `/contact` - اتصل بنا
- `/login` - تسجيل الدخول
- `/admin` - لوحة التحكم (يحتاج تسجيل دخول)

## 🛠️ لوحة التحكم (Admin Dashboard)

بعد تسجيل الدخول، يمكنك:

✅ إدارة الخدمات (إضافة، تعديل، حذف)
✅ إدارة المشاريع (إضافة، تعديل، حذف)
✅ عرض الرسائل من العملاء
✅ إحصائيات شاملة

## 🔧 حل المشاكل الشائعة

### المشكلة: السيرفر لا يعمل
```bash
cd server
rm -rf node_modules package-lock.json
npm install
npm start
```

### المشكلة: الواجهة لا تعمل
```bash
cd client
rm -rf node_modules package-lock.json
npm install
npm start
```

### المشكلة: خطأ في الاتصال بالـ API
تأكد من:
1. السيرفر يعمل على port 5001
2. الواجهة تعمل على port 3000
3. لا يوجد firewall يمنع الاتصال

## 📝 ملاحظات مهمة

- البيانات محفوظة في ملفات JSON في مجلد `server/data/`
- كلمة المرور الافتراضية للأدمن: `admin123`
- يمكنك تغيير الـ PORT من ملف `server/.env`

## 🆘 المساعدة

إذا واجهت أي مشكلة:
1. تأكد من تثبيت Node.js بشكل صحيح
2. تأكد من تشغيل السيرفر أولاً
3. تحقق من console في المتصفح للأخطاء
4. تحقق من terminal للأخطاء في السيرفر

---

✨ **مبروك! موقعك جاهز للاستخدام**
