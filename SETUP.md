# 🚀 تشغيل المشروع - Four Pixels

## خطوات التشغيل السريع

### 1️⃣ تثبيت المكتبات

```bash
# تثبيت مكتبات الباك إند
cd server
npm install

# تثبيت مكتبات الفرونت إند
cd ../client
npm install
```

### 2️⃣ تشغيل الباك إند

```bash
cd server
npm start
```

السيرفر هيشتغل على: http://localhost:5000

### 3️⃣ تشغيل الفرونت إند (في terminal تاني)

```bash
cd client
npm start
```

الموقع هيفتح على: http://localhost:3000

---

## 🔐 إنشاء حساب Admin

استخدم Postman أو Thunder Client:

```
POST http://localhost:5000/api/auth/register
Content-Type: application/json

{
  "email": "admin@fourpixels.com",
  "password": "admin123",
  "role": "admin"
}
```

بعدها تقدر تسجل دخول من الموقع!

---

## 📁 البيانات

كل البيانات محفوظة في ملفات JSON في:
- `server/data/users.json`
- `server/data/services.json`
- `server/data/projects.json`
- `server/data/messages.json`

---

## ✅ المميزات الجاهزة

✨ دعم عربي/إنجليزي كامل
🌓 وضع فاتح/داكن
🎨 أنيميشن سلس
📱 متجاوب تمامًا
🔐 نظام تسجيل دخول آمن
⚡ لوحة تحكم Admin
📧 نموذج تواصل

---

## 🎯 الصفحات

- **الرئيسية** - Hero + خدمات + مشاريع
- **الخدمات** - عرض كل الخدمات
- **المشاريع** - Portfolio مع فلاتر
- **من نحن** - معلومات الشركة
- **التواصل** - نموذج + روابط سوشيال ميديا
- **تسجيل الدخول** - للأدمن
- **لوحة التحكم** - إدارة كل شيء

---

## 🛠️ تعديل البيانات

### إضافة خدمة جديدة
افتح `server/data/services.json` وأضف:

```json
{
  "id": "4",
  "title": {
    "en": "Service Name",
    "ar": "اسم الخدمة"
  },
  "description": {
    "en": "Description",
    "ar": "الوصف"
  },
  "price": 1000,
  "deliveryTime": "1 week",
  "features": {
    "en": ["Feature 1", "Feature 2"],
    "ar": ["ميزة 1", "ميزة 2"]
  },
  "active": true
}
```

### إضافة مشروع جديد
افتح `server/data/projects.json` وأضف:

```json
{
  "id": "7",
  "title": {
    "en": "Project Name",
    "ar": "اسم المشروع"
  },
  "description": {
    "en": "Description",
    "ar": "الوصف"
  },
  "category": "Web Development",
  "tags": ["React", "Node.js"],
  "images": ["https://via.placeholder.com/800x600"],
  "featured": true
}
```

---

## 📦 رفع المشروع

### Frontend (Vercel/Netlify)
```bash
cd client
npm run build
# ارفع مجلد build
```

### Backend (Render/Railway)
```bash
# ارفع مجلد server كامل
# متنساش تحط environment variables
```

---

## 💡 ملاحظات

- ❌ مش محتاج MongoDB خالص
- ✅ كل حاجة شغالة local
- 📝 البيانات في JSON files
- 🔄 تقدر تعدل البيانات يدوي أو من لوحة التحكم

---

بالتوفيق! 🚀
